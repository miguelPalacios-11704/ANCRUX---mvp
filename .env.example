# Clave maestra para key wrapping (32 bytes en base64)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
MASTER_KEY_BASE64=your_master_key_base64_here

# Configuración del nodo Lightning Network (LND)
LND_URL=https://your-lnd-node.example.com:8080
LND_MACAROON_HEX=your_macaroon_in_hex_format

# Certificado TLS del nodo LND (opcional, solo para self-signed certificates)
# Convertir con: cat tls.cert | base64
LND_TLS_CERT_B64=your_tls_cert_base64_here
